# OJ config for debugging causal_conv1d NPU coreDim overflow (issue #981).
# Consumed by .github/workflows/oj.yml. Delete the whole tests/oj/ dir before merging.
runner: npu-a2
pytest_args: "-s -v -x"
